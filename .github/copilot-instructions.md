See CLAUDE.md in the repository root for the full project guide (stack, structure, conventions, commands).

Key reminders:
- Zod v4: use `object()`, `string()`, `output<T>` — NOT `z.object()`, `z.infer`
- CSS Modules only — no inline styles
- Do not edit `src/routeTree.gen.ts` — it is auto-generated
